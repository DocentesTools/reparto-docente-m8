"""Tests that need a real server; excluded from the default run.

``pytest.ini`` adds ``--ignore=tests/live``, so nothing here runs (or counts
towards coverage) unless it is asked for explicitly. See the module docstrings
for what each one needs.
"""
